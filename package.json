{
  "name": "dynamodb-migrations-cdk",
  "version": "0.1.0",
  "main": "dist/index.js",
  "publishConfig": {
    "access": "public"
  },
  "types": "dist/index.d.ts",
  "files": [
    "dist/**/*",
    "README.md",
    "package.json"
  ],
  "scripts": {
    "build": "tsc",
    "prepublishOnly": "npm run build",
    "test": "mocha --require ts-node/register --recursive 'test/integration/**/*.test.ts' --timeout 10000",
    "test:integration": "mocha --require ts-node/register --recursive 'test/integration/**/*.test.ts' --timeout 10000"
  },
  "keywords": [
    "dynamodb",
    "cdk",
    "aws",
    "migration",
    "local",
    "testing"
  ],
  "author": "Recodify",
  "license": "ISC",
  "description": "CDK to Dynamodb-local bridge. Deploy your cdk defined tables locally, with zero drift",
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.632.0",
    "aws-cdk": "^2.1027.0",
    "aws-cdk-lib": "^2.213.0",
    "constructs": "^10.4.2"
  },
  "devDependencies": {
    "@types/randomstring": "^1.3.0",
    "@types/chai": "^4.3.0",
    "@types/mocha": "^10.0.0",
    "@types/node": "^24.3.0",
    "chai": "^4.3.0",
    "mocha": "^10.2.0",
    "randomstring": "^1.3.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.2"
  }
}
